// CJS stub for rehype-raw — used only in the Jest test environment.
// rehype-raw is ESM-only; this stub is a no-op passthrough so the
// plugin array in MessageBubble doesn't throw during tests.
function rehypeRaw() {}

module.exports = rehypeRaw;
module.exports.default = rehypeRaw;
